import { Container, LogoContainer, PresentationContainer } from "./styled"

export const Welcome = () => {
  return (
    <Container>
      <LogoContainer />
      <PresentationContainer>
      </PresentationContainer>
    </Container>
  )
}
